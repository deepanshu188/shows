# Bunjs + SQLite REST API

## Auth
- `/login` ( User login )
- `/signup` ( Signup new user )

## Shows
- `/shows-by-user` ( Get all shows from user )
- `/all-shows` ( Get all shows )
- `/add-show` ( Add new show )
